{
  "name": "game-achieve",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ACHIEVE — Write The Vision. A gamified vision board built on a Be>Do>Have identity engine, scripture (KJV) as the mainframe, and peer-reviewed behavioural neuroscience as the mechanics.",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/unit/",
    "test:e2e": "node build.mjs && node --test tests/e2e/",
    "test:all": "npm run build && node --test tests/unit/ && node --test tests/e2e/",
    "serve": "node scripts/serve.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
